#!/bin/bash
set -e

echo "🧹 Cleaning up residual files for mpn-core..."

if [ -f "/lib/systemd/system/mpn-core.service" ]; then
    rm -f /lib/systemd/system/mpn-core.service
fi

systemctl daemon-reload || true
exit 0
